## 📘 Documentação do Projeto – Validação de Formulário com DOM
## 📝 Visão Geral

Este projeto tem como objetivo explorar o DOM (Document Object Model) por meio da criação e validação de um formulário utilizando JavaScript puro. O sistema valida os campos nome, e-mail e telefone, garantindo que apenas dados corretos possam ser enviados.
O trabalho segue exatamente todas as etapas solicitadas na atividade, incluindo o fluxo de versionamento com Git e a demonstração funcional das validações.

## 🚀 Funcionalidades Implementadas

```
✔️ Validação do campo Nome

Mínimo de 5 caracteres

Máximo de 40 caracteres

Exibe mensagem de erro caso a regra não seja atendida

✔️ Validação do campo E-mail

Verifica se contém o caractere obrigatório @

Exibe aviso caso esteja ausente

✔️ Validação do campo Telefone

Aceita exatamente 11 dígitos (formato: DDD + número do celular)

Bloqueia envio caso esteja incompleto ou com tamanho incorreto

✔️ Validação de Campos Vazios

Nenhum campo pode ser enviado sem preenchimento

O aviso de erro é exibido dinamicamente no DOM
```

## 🧩 Estrutura do Projeto

```
/
│── index.html        # Estrutura do formulário
│── script.js         # Lógica de validação via DOM
│── README.md         # Descrição do projeto
```

## 🛠️| Tecnologias Utilizadas

```
HTML5

CSS3 (simples embutido)

JavaScript (DOM)

Git / GitHub para versionamento
```

## 🌿| Fluxo Git Utilizado conforme solicitado

```
Criada a branch:

git checkout -b DOM

Desenvolvimento completo do formulário e das validações

Commits realizados na branch DOM:

git add .

git commit -m "Implementação da validação DOM"

Envio da branch para o repositório remoto:

git push origin DOM

Merge para a branch principal (main):

Via Pull Request
```

## 🧪| Demonstrações e Funcionamento


## 🎯| Conclusão

Este projeto aplica de forma prática os conceitos de manipulação do DOM, validação de formulários, e versionamento com Git, atendendo integralmente aos critérios da atividade. O código é simples, funcional e fácil de entender, ideal para fins didáticos e para demonstrar domínio dos fundamentos de desenvolvimento web.
